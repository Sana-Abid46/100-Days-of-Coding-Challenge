// Add a Special Number: Make a program that creates custom adders. These adders can add a specific number to any other number you give them later.


function createAdder(num: number) {
    return 10 + num;
};
console.log(createAdder(5)); //15
console.log(createAdder(10)); //20
console.log(createAdder(25)); //35

// I made a function (magic box) that adds 10 to any number.
